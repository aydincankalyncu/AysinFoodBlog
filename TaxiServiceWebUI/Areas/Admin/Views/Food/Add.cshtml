@model TaxiServiceWebUI.Areas.Admin.Models.FoodAddViewModel
@{
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<script src="~/js/ckeditor/ckeditor.js" type="text/javascript"></script>
@*<script>
        $(document).ready(function () {
            CKEDITOR.replace('FoodDescription',
                {
                    allowedContent: true,
                    height: '400px',
                    width: '100%',
                    filebrowserUploadUrl: 'UploadImage'
                });
        });
    </script>*@

<div class="card card-info">
    <div class="card-header">
        <h3 class="card-title"> Yemek Ekleme Formu</h3>
    </div>

    <form class="form-horizontal" asp-controller="Food" asp-action="Add" method="post" enctype="multipart/form-data">
        <div class="card-body">
            <div class="form-group row">
                <label asp-for="Food.FoodTitle" class="col-sm-2 col-form-label"> Yemek Başlığı</label>
                <div class="col-sm-10">
                    <input type="text" asp-for="Food.FoodTitle" class="form-control" placeholder="Yemek ismini giriniz.">
                </div>
                <span style="margin-left:5px; color:red; font-weight:bold;" asp-validation-for="Food.FoodTitle"></span>
            </div>
            <div class="form-group row">
                <label asp-for="Food.FoodImage" class="col-sm-2 col-form-label"> Yemek Resmi</label>
                <div class="col-sm-10">
                    <input type="file" name="file" width="80">
                </div>
            </div>
            <div class="form-group row">
                <label class="col-sm-2 col-form-label" asp-for="Food.CategoryId">Yemek Kategorisi Seçiniz</label>
                <div class="col-sm-10">
                    <select class="form-control" asp-for="Food.CategoryId" asp-items="@ViewBag.Categories">
                        <option selected>Seçiniz</option>
                    </select>
                </div>
            </div>
            <div class="form-group row">
                <label class="col-sm-2 col-form-label" asp-for="Food.FoodDescription">Yemek Detayını Ekleyiniz.</label>
                <div class="col-sm-10">
                    @Html.TextAreaFor(model => model.Food.FoodDescription, new { @id = "editor1" })
                </div>
            </div>
            <div class="card-footer">
                <button type="submit" class="btn btn-info">Kaydet</button>
                <a href="@Url.Action("Index","Food", new { area = "Admin" })" class="btn btn-default float-right">Geri Dön</a>
            </div>
        </div>

    </form>
</div>


<script>
    CKEDITOR.replace('editor1', {
        allowedContent: true,
        height: '400px',
        width: '100%',
        filebrowserUploadUrl: 'UploadImage'
    });
</script>